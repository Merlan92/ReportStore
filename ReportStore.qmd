---
title: "My First Project"
format: dashboard
echo: false
---

# Information About BM Electronics
![BM Electronics](https://avatars.mds.yandex.net/i?id=92d9463f247a088e92d27271bbe5806f_sr-5023418-images-thumbs&n=13.png){height=300 width=400}

[Перейти на сайт BM Electronics](https://bme.uz/)


```{python}
#| echo: false
import folium
import pandas as pd
import matplotlib.pyplot as plt
from IPython.display import display
from itables import show
from folium.plugins import Search
from io import BytesIO
from PIL import Image
import requests
from itables import show

df['status'] = df['status'].map({True: 'Активен', False: 'Не активен'})
output_df = df[['organisation', 'status', 'type', 'id', 'region', 'city']].reset_index(drop=True)

show(output_df)
```


# Info About The Received Data


```{python}
#| content: valuebox
#| title: "Данные магазинов"
#| echo: false

show(styled_df);
```

```{python}
#| layout-nrow: 1
#| layout-ncol: 2

#| content: valuebox
#| title: "Неактивные магазины"
#| icon: house
#| color: red
dict(value=int(df['status'].eq('Не Активен').sum()));
```


```{python}
#| content: valuebox
#| title: "Активные магазины"
#| icon: house
#| color: green
dict(value=int(df['status'].eq('Активен').sum()));

```

# Charts

```{python}
#| echo: false
status_counts = df['status'].value_counts()
colors = ['blue', 'red']
labels = ['Активные', 'Неактивные']

plt.figure(figsize=(8, 8))
plt.pie(
    status_counts, 
    labels=labels, 
    autopct='%1.1f%%', 
    startangle=140, 
    colors=colors,
    wedgeprops={'edgecolor': 'black'}
)
plt.title('Процент активных и неактивных магазинов', fontsize=16, fontweight='bold')
plt.show();
```


```{python}

#| echo: false
region_active_status = df.groupby(['region', 'status'])['id'].count().unstack(fill_value=0)

colors = ['red', 'blue']

ax = region_active_status.plot(
    kind='bar',
    stacked=False,
    color=colors,
    figsize=(13, 8),
    edgecolor='black',
    width=0.8
)

for container in ax.containers:
    ax.bar_label(container, fmt='%d', fontsize=12, padding=3)

plt.title('Активные и неактивные магазины по регионам', fontsize=16, fontweight='bold')
plt.xlabel('Регион', fontsize=14)
plt.ylabel('Количество магазинов', fontsize=14)
plt.xticks(rotation=45, fontsize=12)
plt.yticks(fontsize=12)
plt.legend(['Неактивные', 'Активные'], title='Статус', fontsize=12)
plt.tight_layout()
plt.show();

```


# Map
```{python}
#| echo: false


import folium
from folium.plugins import Search
import pandas as pd


if df.empty:
    my_map = folium.Map(location=[41.3775, 64.5853], zoom_start=5)
else:
    
    df = df.dropna(subset=['lat', 'long'])

   
    df['lat'] = pd.to_numeric(df['lat'], errors='coerce').fillna(0)
    df['long'] = pd.to_numeric(df['long'], errors='coerce').fillna(0)

    map_center = [
        df['lat'].mean() if df['lat'].sum() != 0 else 41.3775,
        df['long'].mean() if df['long'].sum() != 0 else 64.5853
    ]
    my_map = folium.Map(location=map_center, zoom_start=5)

    marker_group = folium.FeatureGroup(name="Stores").add_to(my_map)

    
    color_map = {'Активен': "green", 'Не Активен': "red"}

    for _, row in df.iterrows():
        store_status = row['status']
        marker_color = color_map.get(store_status, "gray")

        folium.Marker(
            location=[row['lat'], row['long']],
            popup=f"""
                <div style="
                    font-family: Arial, sans-serif;
                    font-size: 14px;
                    color: #333;
                    padding: 8px;
                    border-radius: 8px;
                    background: #fff;
                    width: 200px;
                ">
                    <b>ID Магазина:</b> {row['id']}<br>
                    <b>Клиент:</b> {row['organisation']}<br>
                    <b>Регион:</b> {row['region']}<br>
                    <b>Статус:</b> {'🟢 Активный' if store_status == 'Активен' else '🔴 Неактивный'}
                </div>
            """,
            icon=folium.Icon(color=marker_color, icon='home'),
            name=row['id']
        ).add_to(marker_group)

 
    Search(
        layer=marker_group,
        search_label="name",
        placeholder="Поиск по ID магазина",
        collapsed=False
    ).add_to(my_map)


my_map.save("map.html");


```

<iframe src="map.html" width="100%" height="800px"></iframe>
